// Generated by CoffeeScript 1.9.0
(function() {
  var forEachEdge;

  forEachEdge = require('./FaceVertexMeshTraversion').forEachEdge;

  module.exports = function(faceVertexMesh) {
    var count, edge, edgeCountMap;
    edgeCountMap = {};
    forEachEdge(faceVertexMesh, function(v1, v2) {
      var a, b, key;
      a = Math.min(v1, v2);
      b = Math.max(v1, v2);
      key = a + '-' + b;
      if (edgeCountMap[key] == null) {
        edgeCountMap[key] = 0;
      }
      return edgeCountMap[key]++;
    });
    for (edge in edgeCountMap) {
      count = edgeCountMap[edge];
      if (count !== 2) {
        return false;
      }
    }
    return true;
  };

}).call(this);
